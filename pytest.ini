[pytest]
# Custom markers — keeps `pytest` warning-free.
markers =
    integration: requires a live DB (reads creds from .env). Opt-in only.

# Default run excludes integration tests so the suite stays network-free.
# Run them explicitly with:  pytest -m integration
addopts = -m "not integration"
